function firstDigit(string) {
  var result = [];
  var sortDigit = string.split("");
  for (var i = 0; i < sortDigit.length; i++) {
    if (sortDigit[i] != isNaN) {
    } else sortDigit[i] == $;
  }
  return result.join("");
}
console.log(firstDigit("py3thon"));
function replaceNumber(input_str) {
  return input_str.replace(/[0-9]/, "$");
}
console.log(replaceNumber("phuc3"));
