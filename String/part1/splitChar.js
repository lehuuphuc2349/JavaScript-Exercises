const splitChar = (string, number) => {
  var temp = string;
  var finalResult = new Array();
  var result = temp.split("");
  for (var i = 0; i < result.length; i += number) {
    if (i < i + 1) {
    }
  }
  return finalResult;
};
console.log(splitChar("phucdeptrai", 2));
