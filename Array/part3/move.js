const move = (start, end) => {};
const converse = (array, start, end) => {
  var result = new Array();
  return result.toString().substring(start, end).reverse();
};

var temp = [1, 2, 3, 4, 5];
var result = temp.toString().split(",");
console.log(result);
